# React 前端開發快速參考

> 給有 Vue 經驗的 TypeScript 開發者

## 📖 關於本教學

這是一系列快速參考指南，專為熟悉 TypeScript 和 Vue.js 的開發者設計，幫助你快速掌握 React 核心概念與最佳實踐。所有範例都來自 Waterball Software Academy 專案的實際程式碼。

**學習目標**：能夠理解並回答 [`interview-frontend.md`](../interview/interview-frontend.md) 中的 50 道前端技術面試題。

---

## 🗺️ 學習路徑

建議按照以下順序學習：

### 1. [React Hooks 快速指南](./01-react-hooks.md) ⭐ **最重要**

掌握 React 的核心：useState、useEffect、useCallback、useMemo、useRef 與 Custom Hooks。

- 📝 對應面試題：Q1-Q15, Q21-Q30
- ⏱️ 預計閱讀時間：15 分鐘

### 2. [TypeScript + React 型別技巧](./02-typescript-react.md)

學習 React 中的 TypeScript 進階型別應用。

- 📝 對應面試題：Q46-Q50
- ⏱️ 預計閱讀時間：10 分鐘

### 3. [Context API 與狀態管理](./03-context-api.md)

理解 React 的狀態管理方式與 Context 設計模式。

- 📝 對應面試題：Q7, Q21-Q30
- ⏱️ 預計閱讀時間：12 分鐘

### 4. [表單處理](./04-form-handling.md)

掌握 React Hook Form + Zod 的表單驗證模式。

- 📝 對應面試題：Q16-Q20
- ⏱️ 預計閱讀時間：10 分鐘

### 5. [非同步處理模式](./05-async-patterns.md)

學習 React 中處理 API 呼叫的最佳實踐。

- 📝 對應面試題：Q14, Q31-Q40
- ⏱️ 預計閱讀時間：12 分鐘

### 6. [Tailwind CSS & 無障礙](./06-tailwind-a11y.md)

快速掌握 Tailwind CSS 工具函式與無障礙設計。

- 📝 對應面試題：Q20, Q41-Q45
- ⏱️ 預計閱讀時間：10 分鐘

---

## 🆚 Vue vs React 快速對照表

給有 Vue 3 經驗的開發者，以下是核心概念的對照：

| 功能                | Vue 3                      | React                                     |
| ------------------- | -------------------------- | ----------------------------------------- |
| **響應式狀態**      | `ref()`, `reactive()`      | `useState()`                              |
| **計算屬性**        | `computed()`               | `useMemo()`                               |
| **監聽變化**        | `watch()`, `watchEffect()` | `useEffect()`                             |
| **生命週期 - 掛載** | `onMounted()`              | `useEffect(() => {}, [])`                 |
| **生命週期 - 卸載** | `onUnmounted()`            | `useEffect(() => { return cleanup }, [])` |
| **跨元件狀態**      | `provide()` / `inject()`   | Context API                               |
| **效能優化函式**    | —                          | `useCallback()`                           |
| **可變引用**        | `ref()` (DOM)              | `useRef()`                                |
| **表單處理**        | `v-model`                  | Controlled Components                     |

### 💡 關鍵差異

1. **響應式系統**

   - **Vue**: 自動追蹤依賴，修改 `ref.value` 就會自動更新
   - **React**: 顯式呼叫 `setState`，不可直接修改 state

2. **依賴追蹤**

   - **Vue**: 編譯時自動分析依賴（`computed`、`watch` 不需手動聲明）
   - **React**: 手動在依賴陣列中聲明（`useEffect`、`useCallback` 需明確列出）

3. **更新機制**
   - **Vue**: 精準追蹤，只更新變化的部分
   - **React**: 元件重新渲染，依賴 memo/useMemo/useCallback 優化

---

## 📚 配合面試題使用

每個主題都標註了相關的面試題編號，建議學習流程：

1. **閱讀基本語法** - 快速瀏覽每個 Hook/概念的用法
2. **查看專案實例** - 理解實際專案中如何應用
3. **思考常見陷阱** - 避免踩坑
4. **回答面試題** - 回到 `interview-frontend.md` 測試理解

---

## 📁 專案結構速查

前端程式碼位於 `www_root/waterballsa-frontend/src/`

```
src/
├── contexts/          # Context Providers (全域狀態管理)
│   ├── auth-context.tsx
│   ├── user-purchase-context.tsx
│   └── journey-context.tsx
│
├── hooks/             # Custom Hooks (邏輯復用)
│   ├── use-video-progress.ts
│   ├── use-mission.ts
│   └── use-api.ts
│
├── components/        # React 元件
│   ├── ui/           # 基礎 UI 元件 (shadcn/ui)
│   └── auth/         # 驗證相關元件
│
└── lib/              # 工具函式與 API
    ├── api/          # API client 實作
    └── utils.ts      # 通用工具函式 (如 cn())
```

---

## 🎯 學習建議

### 給 Vue 開發者的建議

1. **不要過度類比**
   雖然 Vue 和 React 有相似概念，但底層思維不同。React 更偏向「函式式」思維。

2. **重點理解依賴陣列**
   這是 Vue 開發者最容易混淆的地方。Vue 自動追蹤依賴，React 需要手動聲明。

3. **接受「重新渲染」**
   React 元件會頻繁重新執行，這是正常的。不要想著「阻止渲染」，而是用 memo 優化。

4. **狀態不可變更新**
   React 中不能 `state.value++`，必須 `setState(state + 1)`。不可變性是核心原則。

### 除錯技巧

```typescript
// 在 useEffect 中加 console.log 追蹤執行
useEffect(() => {
	console.log('Effect 執行了！依賴:', { user, isLoading })
	// ...
}, [user, isLoading])

// 用 React DevTools 查看元件樹與 props
// Chrome Extension: React Developer Tools
```

---

## ❓ 常見問題

### Q: 為什麼 useEffect 會執行兩次？

A: React 18 的 Strict Mode 會在開發環境中**故意**讓 effects 執行兩次，幫助你發現潛在問題。生產環境不會這樣。

### Q: 依賴陣列到底要不要加？

A: **一定要加！** 規則是：如果 useEffect/useCallback 中使用了 props 或 state，就必須加入依賴陣列。ESLint 會幫你檢查。

### Q: Vue 的 `v-model` 在 React 怎麼寫？

A: React 叫做 Controlled Components：

```tsx
// Vue
<input v-model="name" />

// React
<input
  value={name}
  onChange={(e) => setName(e.target.value)}
/>
```

---

## 🚀 開始學習

準備好了嗎？從 [React Hooks 快速指南](./01-react-hooks.md) 開始吧！

---

## 📖 延伸資源

- [React 官方文件（中文）](https://zh-hant.react.dev/)
- [TypeScript 官方文件](https://www.typescriptlang.org/docs/)
- [React Hook Form 官方文件](https://react-hook-form.com/)
- [Zod 官方文件](https://zod.dev/)
- [Tailwind CSS 官方文件](https://tailwindcss.com/docs)
